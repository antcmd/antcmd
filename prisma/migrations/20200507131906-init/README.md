# Migration `20200507131906-init`

This migration has been generated by ignatif at 5/7/2020, 1:19:06 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."User" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  )

CREATE TABLE "quaint"."Post" (
"authorId" INTEGER   ,"content" TEXT   ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"numberPlayed" INTEGER NOT NULL DEFAULT 0 ,"title" TEXT NOT NULL  ,FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE)

CREATE TABLE "quaint"."Action" (
"content" INTEGER NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"postId" INTEGER   ,"time" DATE   ,FOREIGN KEY ("postId") REFERENCES "Post"("id") ON DELETE SET NULL ON UPDATE CASCADE)

CREATE TABLE "quaint"."Like" (
"authorId" INTEGER   ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"postId" INTEGER   ,"time" DATE   ,FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE,
FOREIGN KEY ("postId") REFERENCES "Post"("id") ON DELETE SET NULL ON UPDATE CASCADE)

CREATE UNIQUE INDEX "quaint"."User.name" ON "User"("name")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200507131906-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,41 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "sqlite"
+  url      = "file:./dev.db"
+}
+
+model User {
+  id      Int     @default(autoincrement()) @id
+  name    String  @unique
+  posts   Post[]
+  likes   Like[]
+}
+
+model Post {
+  content      String?
+  id           Int     @default(autoincrement()) @id
+  title        String
+  author       User?   @relation(fields: [authorId], references: [id])
+  authorId     Int?
+  actions      Action[]
+  likes        Like[]
+  numberPlayed Int     @default(0)
+}
+
+model Action {
+  id        Int     @default(autoincrement()) @id
+  time      DateTime?
+  content   Int
+}
+
+model Like {
+  id        Int     @default(autoincrement()) @id
+  time      DateTime?
+  author    User?   @relation(fields: [authorId], references: [id])
+  authorId  Int?
+  post      Post?   @relation(fields: [postId], references: [id])
+  postId    Int?
+}
```


