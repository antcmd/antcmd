# Migration `20200507171112-inait`

This migration has been generated by ignatif at 5/7/2020, 5:11:12 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Action" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"postId" INTEGER   ,"time" DATE   ,FOREIGN KEY ("postId") REFERENCES "Post"("id") ON DELETE SET NULL ON UPDATE CASCADE)

INSERT INTO "quaint"."new_Action" ("id", "postId", "time") SELECT "id", "postId", "time" FROM "quaint"."Action"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Action";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Action" RENAME TO "Action";

CREATE TABLE "quaint"."new_Like" (
"authorId" INTEGER   ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"postId" INTEGER   ,FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE,
FOREIGN KEY ("postId") REFERENCES "Post"("id") ON DELETE SET NULL ON UPDATE CASCADE)

INSERT INTO "quaint"."new_Like" ("authorId", "id", "postId") SELECT "authorId", "id", "postId" FROM "quaint"."Like"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Like";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Like" RENAME TO "Like";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200507131906-init..20200507171112-inait
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:./dev.db"
 }
 model User {
   id      Int     @default(autoincrement()) @id
@@ -27,14 +27,12 @@
 model Action {
   id        Int     @default(autoincrement()) @id
   time      DateTime?
-  content   Int
 }
 model Like {
   id        Int     @default(autoincrement()) @id
-  time      DateTime?
   author    User?   @relation(fields: [authorId], references: [id])
   authorId  Int?
   post      Post?   @relation(fields: [postId], references: [id])
   postId    Int?
```


